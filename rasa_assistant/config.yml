recipe: default.v1
language: pt
assistant_id: wpp_dental_calm

pipeline:
  - name: CompactLLMCommandGenerator
    llm:
      model_group: openai_command_generator
    flow_retrieval:
      embeddings:
        model_group: openai_embeddings
    user_input:
      max_characters: 420

policies:
  - name: FlowPolicy
